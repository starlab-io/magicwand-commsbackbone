version: '2'
services:
  apache:
    # image: patricknevindwyer/apache-harness:latest
    image: patricknevindwyer/apacheperf:latest
    environment:
      LORIS_TEST_DURATION:
    ports:
      - "9000:80"
    volumes:
      - ./log/apacheperf:/var/log/apacheperf
  goloris:
    image: patricknevindwyer/goloris-harness:latest
    environment:
      LORIS_TEST_DURATION:
    depends_on:
      - apache
    links:
      - apache
    command: /bin/bash -c 'sleep 20 ; /goloris -victimUrl="http://apache"'
  httperf:
    image: patricknevindwyer/httperf:latest
    environment:
      LORIS_TEST_DURATION:
    depends_on:
      - apache
    links:
      - apache
    volumes:
      - ./log/httperf:/var/log/httperf